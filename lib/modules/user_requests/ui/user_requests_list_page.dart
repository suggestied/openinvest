import 'package:flutter/material.dart';

class UserRequestsListPage extends StatelessWidget {
  const UserRequestsListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('User requests list page coming soon...'),
    );
  }
}